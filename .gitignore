# Private, machine-local deployment context — never commit
PRIVATE-CONTEXT.md
*.local.*

# Agent-local settings that may hold credentials or tokens
.claude/settings.local.json

# Dependencies
node_modules

# Build / adapter output
/.svelte-kit
/build
.output
.vercel
.netlify
.wrangler

# Test, coverage & Playwright artifacts
/coverage
/test-results
/playwright-report
/blob-report
/playwright/.cache

# Env files (placeholders only ever committed, as .env.example)
.env
.env.*
!.env.example

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# OS / editor cruft
.DS_Store
Thumbs.db
*.swp
